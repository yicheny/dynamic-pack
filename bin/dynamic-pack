#!/usr/bin/env node
'use strict';

const process = require('process')
const dpError = require('../lib/DPError');
const createCLI = require('../lib/cli');
const tryExecute = require('../lib/tryExecute');

tryExecute(
    () => {
        createCLI().execute(process.argv);
    },
    null,
    ()=>dpError.createLog());
